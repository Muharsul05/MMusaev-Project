spring:
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=magarusik
    username: postgres
    password: 1312

server:
  port: 8080
  servlet:
    context-path: /
